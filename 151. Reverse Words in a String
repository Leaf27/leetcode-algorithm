public class Solution {
    public String reverseWords(String s) {
        String[] piece = s.split(" ");
        List<String> list = new ArrayList<>();
        for(int i = piece.length-1;i>=0;i--) if(!piece[i].equals("")) list.add(piece[i]);
        return String.join(" ",list);
    }
}
